node_modules/
dist/
.env
.env.local

# Dataset is large, keep it out of the repo
data/HHGOA_IEEE/*
!data/HHGOA_IEEE/.gitkeep

# cases/ holds the 20 answer files and IS the graded deliverable, so it is
# committed, not ignored.

# Next.js
ui/.next/
ui/out/
ui/node_modules/

# Prepared load files, regenerated by npm run prep:full
graph/load/prepared/

# Runtime approval records, written by the UI permission gate
config/approvals.json

# LLM response cache: makes re-running the benchmark free
.llm-cache/
