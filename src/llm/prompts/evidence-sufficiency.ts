// Prompt template: evidence sufficiency (sufficient + missing_evidence + requested_action out).
// Prompt text lives here, never inlined in orchestrator or tool code.
// Placeholder: written in the LLM-layer phase of build_plan.md.

export const EVIDENCE_SUFFICIENCY_PROMPT = '';
