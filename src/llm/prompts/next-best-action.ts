// Prompt template: next-best-action selection (14 policy actions, each with a route and a rule citation).
// Prompt text lives here, never inlined in orchestrator or tool code.
// Placeholder: written in the LLM-layer phase of build_plan.md.

export const NEXT_BEST_ACTION_PROMPT = '';
