// Prompt template: pattern identification (evidence bundle in, patterns_matched + confidence + rationale out).
// Prompt text lives here, never inlined in orchestrator or tool code.
// Placeholder: written in the LLM-layer phase of build_plan.md.

export const PATTERN_IDENTIFICATION_PROMPT = '';
