// Prompt template: risk assessment (patterns + prior case outcomes in, risk_level + confidence + key_factors out).
// Prompt text lives here, never inlined in orchestrator or tool code.
// Placeholder: written in the LLM-layer phase of build_plan.md.

export const RISK_ASSESSMENT_PROMPT = '';
