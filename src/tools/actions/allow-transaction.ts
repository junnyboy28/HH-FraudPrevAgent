// Mock action: ALLOW_TRANSACTION (route auto per config/permissions.json).
// Placeholder: implemented in the actions phase of build_plan.md.

export const ACTION_ID = 'ALLOW_TRANSACTION';

export function allowTransaction(): never {
  throw new Error('ALLOW_TRANSACTION is not implemented yet');
}
