// Mock action: BLOCK_CARD (route L1, or L2 above $2,500 exposure per config/permissions.json).
// Placeholder: implemented in the actions phase of build_plan.md.

export const ACTION_ID = 'BLOCK_CARD';

export function blockCard(): never {
  throw new Error('BLOCK_CARD is not implemented yet');
}
