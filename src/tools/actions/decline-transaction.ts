// Mock action: DECLINE_TRANSACTION (route L1 per config/permissions.json).
// Placeholder: implemented in the actions phase of build_plan.md.

export const ACTION_ID = 'DECLINE_TRANSACTION';

export function declineTransaction(): never {
  throw new Error('DECLINE_TRANSACTION is not implemented yet');
}
