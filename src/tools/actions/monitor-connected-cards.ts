// Mock action: MONITOR_CONNECTED_CARDS (route auto per config/permissions.json).
// Placeholder: implemented in the actions phase of build_plan.md.

export const ACTION_ID = 'MONITOR_CONNECTED_CARDS';

export function monitorConnectedCards(): never {
  throw new Error('MONITOR_CONNECTED_CARDS is not implemented yet');
}
