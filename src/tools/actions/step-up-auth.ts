// Mock action: STEP_UP_AUTH (route auto per config/permissions.json).
// Placeholder: implemented in the actions phase of build_plan.md.

export const ACTION_ID = 'STEP_UP_AUTH';

export function stepUpAuth(): never {
  throw new Error('STEP_UP_AUTH is not implemented yet');
}
