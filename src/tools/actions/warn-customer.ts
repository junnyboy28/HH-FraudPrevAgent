// Mock action: WARN_CUSTOMER (route auto per config/permissions.json).
// Placeholder: implemented in the actions phase of build_plan.md.

export const ACTION_ID = 'WARN_CUSTOMER';

export function warnCustomer(): never {
  throw new Error('WARN_CUSTOMER is not implemented yet');
}
