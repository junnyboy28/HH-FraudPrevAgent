// Case view: evidence timeline, pattern matches, risk and confidence, reasoning
// trace, recommended actions with approve/reject for gated ones, case status.
// Placeholder: built in the UI phase of build_plan.md.
export default function CasePage() {
  return <main className="p-8" />;
}
